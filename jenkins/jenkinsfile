pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {           
                echo 'Building'
                sh ''
                sh 'pip install -r requirements.txt'
                sh 'python3 app.py'
            }
        }
    }
}