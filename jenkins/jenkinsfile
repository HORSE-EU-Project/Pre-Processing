pipeline {
    agent none
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {           
                echo 'Building'
                sh 'pip install -r requirements.txt'
                sh 'python app.py'
            }
        }
    }
}